[Unit]
Description=Honeygain Linux Docker Container
Wants=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker run --rm --name honeygain-docker -d honeygain/honeygain

[Install]
WantedBy=multi-user.target
